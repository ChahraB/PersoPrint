{
  "name": "teecustomizer",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cp -r build ../server/client/build",
    "start": "cd server && npm install && npm start"
  }
}
